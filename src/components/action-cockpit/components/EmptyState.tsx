/**
 * Empty State Component
 * Displayed when there are no pending actions
 */

import React from 'react';

export const EmptyState: React.FC = () => {
  return null; // We don't render anything when empty
};

